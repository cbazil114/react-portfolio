import React from 'react';
// import NavTabs from '../NavTabs';

export default function Header (props) {
    // const { currentPage, setCurrentPage } = props;
    return (
        <header>
            <div>
                <h1>React Portfolio - Connor Bazil</h1>
            </div>
        </header>
    )
}

